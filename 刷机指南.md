# OnePlus 6 AGNOS 刷写完整指南

## 目录
1. [前言](#前言)
2. [准备工作](#准备工作)
3. [刷机前检查](#刷机前检查)
4. [刷机流程](#刷机流程)
5. [刷机后验证](#刷机后验证)
6. [固件说明](#固件说明)
7. [故障排除](#故障排除)
8. [常见问题](#常见问题)

---

## 前言

本文档提供从零开始为 OnePlus 6 (enchilada) 刷写 AGNOS (Android-Guided Native Operating System) 的完整指南。AGNOS 是基于 Ubuntu 的定制系统，专为 openpilot 自动驾驶项目设计。

### 系统要求

- **设备**: OnePlus 6 (型号 A6000/A6003)
- **操作系统**: Ubuntu 20.04 或更高版本 (用于构建)
- **电脑**: Linux 系统 (推荐 Ubuntu)
- **USB 数据线**: 高质量数据线

### 重要警告

⚠️ **刷机有风险，操作需谨慎！**

- 刷机将清除设备上的所有数据
- 可能导致设备变砖
- 可能失去官方保修
- 请确保完全理解每个步骤后再操作

### 移植状态

当前 OnePlus 6 AGNOS 移植已完成基础构建和固件集成：

| 组件 | 状态 | 说明 |
|--------|------|------|
| 内核 | ✅ 完成 | 基于 OnePlus 6 官方内核 |
| 系统镜像 | ✅ 完成 | 所有固件已集成 |
| GPU 固件 | ✅ 完成 | Adreno A630 固件 |
| DSP 固件 | ✅ 完成 | ADSP/CDSP/SDSP 全部集成 |
| WiFi 固件 | ✅ 完成 | QCA6174 固件 |
| 音频固件 | ✅ 完成 | DSP 音频处理固件 |
| 视频编解码 | ✅ 完成 | Venus 固件 |
| DRM 固件 | ✅ 完成 | Widevine DRM 固件 |
| 摄像头 | ⏳ 开发中 | 固件已提取，驱动需适配 IMX519 |

详见 [ONEPLUS6_PORT_STATUS.md](./ONEPLUS6_PORT_STATUS.md)

---

## 准备工作

### 1. 解锁 Bootloader

**步骤 1.1: 启用开发者选项**

1. 进入手机 **设置** > **关于手机**
2. 连续点击 **版本号** 7 次，直到提示"您已处于开发者模式"
3. 返回 **设置** 主界面，进入 **系统** > **开发者选项**

**步骤 1.2: 启用 OEM 解锁和 USB 调试**

1. 在开发者选项中，启用 **OEM 解锁**
2. 启用 **USB 调试**

**步骤 1.3: 解锁 Bootloader**

1. 关闭手机
2. 同时按住 **电源键** 和 **音量减键** 进入 Fastboot 模式
3. 使用 USB 线连接电脑
4. 在电脑上执行以下命令:

```bash
# 检查设备连接
fastboot devices

# 解锁 Bootloader (会清除所有数据)
fastboot oem unlock

# 按手机屏幕上的确认键确认解锁
```

5. 解锁完成后，设备会自动重启
6. 重新进入 Fastboot 模式 (电源键 + 音量减键)

**步骤 1.4: 验证解锁状态**

```bash
# 检查解锁状态
fastboot getvar unlocked
# 应该显示: unlocked: yes
```

### 2. 安装必要的工具

**步骤 2.1: 安装 Android Platform Tools**

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install android-tools-fastboot android-tools-adb

# 验证安装
fastboot --version
adb version
```

**步骤 2.2: 安装构建工具 (如果需要自行构建)**

```bash
# 安装 Docker
sudo apt install docker.io docker-compose
sudo systemctl start docker
sudo systemctl enable docker

# 将当前用户添加到 docker 组
sudo usermod -aG docker $USER
# 需要重新登录或执行: newgrp docker
```

### 3. 准备刷机文件

**步骤 3.1: 获取构建好的镜像文件**

确保以下文件存在于 `/home/ubuntu/oneplus6-openpilot-project/agnos-builder-oneplus6/output/` 目录:

```bash
cd /home/ubuntu/oneplus6-openpilot-project/agnos-builder-oneplus6/output/
ls -lh
```

应该包含:
- `boot.img` (约 15MB) - 内核和设备树
- `system.img` (约 2.7GB) - AGNOS 系统镜像 (包含 wlan.ko 等内核模块)

**步骤 3.2: 备份原始系统 (强烈推荐)**

在刷机前，建议备份原始系统分区:

```bash
# 进入 Fastboot 模式
# 备份 boot 分区
fastboot boot /path/to/TWRP.img

# 在 TWRP 中使用 adb 备份
adb backup -apk -shared -all -f backup.ab
```

---

## 刷机前检查

### 1. 检查设备连接

```bash
# 确保设备在 Fastboot 模式
fastboot devices

# 应该显示设备序列号
```

### 2. 检查设备型号

```bash
# 获取设备信息
fastboot getvar product
fastboot getvar manufacturer

# 应该显示:
# product: enchilada 或 oneplus6
# manufacturer: OnePlus
```

### 3. 验证镜像文件

```bash
cd /home/ubuntu/oneplus6-openpilot-project/agnos-builder-oneplus6/output/

# 检查文件完整性
file boot.img
file system.img

# boot.img 应该显示: Android bootimg
# system.img 应该显示: Android sparse image
```

### 4. 检查电量

确保手机电量至少在 **50% 以上**，避免刷机过程中断电。

### 5. 分区调整 (重要！)

**警告**: OnePlus 6 默认的 system 分区只有 2.8GB，而 AGNOS system.img 约 2.7GB，几乎占满整个分区。为了确保系统正常运行并有足够空间安装额外软件，**必须先调整分区大小**。

#### OnePlus 6 原始分区布局

| 分区名称 | 设备路径 | 扇区范围 | 大小 | 文件系统 |
|---------|---------|---------|------|---------|
| system_a | /dev/sda13 | 85824-817983 | 2.8 GiB | ext4 |
| system_b | /dev/sda14 | 817984-1550143 | 2.8 GiB | ext4 |
| odm_a | /dev/sda15 | 1550144-1575743 | 100 MiB | ext4 |
| odm_b | /dev/sda16 | 1575744-1601343 | 100 MiB | ext4 |
| userdata | /dev/sda17 | 1601344-30437370 | 110 GiB | ext4 |

#### 调整后的分区布局

| 分区名称 | 设备路径 | 扇区范围 | 大小 | 变化 |
|---------|---------|---------|------|------|
| system_a | /dev/sda13 | 85824-4194303 | 15 GiB | 2.8GB → 15GB (+12.2GB) |
| system_b | /dev/sda14 | 4194304-8323327 | 15 GiB | 2.8GB → 15GB (+12.2GB) |
| userdata | /dev/sda17 | 8323328-30437370 | 85 GiB | 110GB → 85GB (-25GB) |
| odm_a | 删除 | - | - | 删除 |
| odm_b | 删除 | - | - | 删除 |

#### 分区调整步骤

**步骤 5.1: 进入 TWRP Recovery**

1. 将 TWRP 镜像下载到电脑:
   - TWRP for OnePlus 6: [下载链接](https://dl.twrp.me/enchilada/)
   - 文件名: `twrp-3.3.1-0-enchilada.img` (或最新版本)

2. 启动到 TWRP:
```bash
# 临时启动到 TWRP
fastboot boot twrp-3.3.1-0-enchilada.img
```

**步骤 5.2: 调整分区表**

在 TWRP 终端中执行以下命令:

```bash
# 进入 TWRP 终端
# 在 TWRP 主界面点击 "Advanced" > "Terminal"

# 删除旧的 system_a, system_b, odm_a, odm_b, userdata 分区
sgdisk --delete=13 /dev/block/sda
sgdisk --delete=14 /dev/block/sda
sgdisk --delete=15 /dev/block/sda
sgdisk --delete=16 /dev/block/sda
sgdisk --delete=17 /dev/block/sda

# 创建新的 system_a 分区 (15GB)
sgdisk --new=13:85824:4194303 --change-name=13:system_a --typecode=13:8300 /dev/block/sda

# 创建新的 system_b 分区 (15GB)
sgdisk --new=14:4194304:8323327 --change-name=14:system_b --typecode=14:8300 /dev/block/sda

# 创建新的 userdata 分区 (85GB)
sgdisk --new=17:8323328:30437370 --change-name=17:userdata --typecode=17:8300 /dev/block/sda

# 验证分区表
sgdisk -p /dev/block/sda
```

**步骤 5.3: 格式化新分区**

```bash
# 格式化 system_a 分区
mke2fs -t ext4 /dev/block/by-name/system_a

# 格式化 system_b 分区
mke2fs -t ext4 /dev/block/by-name/system_b

# 格式化 userdata 分区
mke2fs -t ext4 /dev/block/by-name/userdata
```

**步骤 5.4: 重启到 Fastboot**

```bash
# 重启到 Fastboot 模式
reboot bootloader
```

#### 分区调整脚本 (推荐)

项目已提供自动分区调整脚本，推荐使用：

```bash
# 1. 将脚本传输到设备
adb push /home/ubuntu/oneplus6-openpilot-project/agnos-builder-oneplus6/resize_partitions.sh /sdcard/

# 2. 在 TWRP 终端中执行脚本
sh /sdcard/resize_partitions.sh
```

脚本会自动执行以下操作：
1. 删除旧的 system_a, system_b, odm_a, odm_b, userdata 分区
2. 创建新的 system_a 分区 (15GB)
3. 创建新的 system_b 分区 (15GB)
4. 创建新的 userdata 分区 (85GB)
5. 格式化所有新分区
6. 验证分区表

**注意**: 脚本执行完成后，会显示新的分区布局和下一步操作提示。

---

## 刷机流程

### 方式一: 使用一键刷机脚本 (推荐)

**步骤 1: 执行完整刷机脚本**

```bash
cd /home/ubuntu/oneplus6-openpilot-project/agnos-builder-oneplus6/

# 执行一键刷机脚本
./flash_all.sh
```

脚本会自动执行以下操作:
1. 检查 fastboot 工具是否安装
2. 检查设备连接
3. 验证设备型号
4. 格式化 userdata 分区
5. 刷写 boot 镜像
6. 刷写 system 镜像
7. 刷写 vendor 镜像 (如果存在)
8. 重启设备

**步骤 2: 确认刷机**

脚本执行完成后，会询问是否立即重启:

```bash
是否立即重启? (y/n): y
```

输入 `y` 确认重启，设备将自动启动到 AGNOS 系统。

---

### 方式二: 手动分步刷机 (高级用户)

**重要提示**: OnePlus 6 使用 A/B 分区方案，system_a (/dev/sda13) 和 system_b (/dev/sda14) 互为备份。刷机时可以选择刷写到 system_a 或 system_b。

**步骤 1: 格式化分区**

```bash
# 格式化 userdata 分区
fastboot format:ext4 userdata
```

**步骤 2: 刷写 Boot 镜像 (内核)**

```bash
cd /home/ubuntu/oneplus6-openpilot-project/agnos-builder-oneplus6/

# 刷写 boot 分区
fastboot flash boot output/boot.img

# 验证刷写
fastboot getvar version-bootloader
```

**步骤 3: 刷写 System 镜像 (AGNOS)**

```bash
# 刷写 system 分区 (会自动刷写到 system_a)
fastboot flash system output/system.img

# 或者明确指定刷写到 system_a
fastboot flash system_a output/system.img

# 或者刷写到 system_b
fastboot flash system_b output/system.img
```

**步骤 4: 刷写 Vendor 镜像 (可选)**

```bash
# 如果有 vendor.img
if [ -f "output/vendor.img" ]; then
    fastboot flash vendor output/vendor.img
fi
```

**步骤 5: 重启设备**

```bash
# 重启到 AGNOS 系统
fastboot continue

# 或者强制重启
fastboot reboot
```

---

## 刷机后验证

### 1. 首次启动

首次启动 AGNOS 系统可能需要 **3-5 分钟**，请耐心等待。

启动过程中会看到:
- OnePlus Logo
- Ubuntu 启动画面
- AGNOS 桌面环境

### 2. 验证系统启动

**步骤 2.1: 检查 ADB 连接**

```bash
# 等待系统完全启动后
adb devices

# 应该显示设备序列号
```

**步骤 2.2: 检查系统版本**

```bash
# 进入 shell
adb shell

# 查看系统信息
cat /etc/os-release
cat /VERSION

# 应该显示 AGNOS 版本信息
```

**步骤 2.3: 检查硬件支持**

```bash
# 检查显示
adb shell ls /sys/class/drm/

# 检查触摸屏
adb shell ls /dev/input/

# 检查 Wi-Fi
adb shell lsmod | grep wlan
adb shell iwconfig
```

### 3. 验证 Openpilot 功能

```bash
# 检查 openpilot 服务
adb shell systemctl status openpilot

# 检查摄像头
adb shell ls /dev/video*

# 检查传感器
adb shell ls /sys/bus/iio/devices/
```

---

## 固件说明

### 集成的固件清单

AGNOS system.img 已集成以下 OnePlus 6 专用固件：

#### GPU 固件 (Adreno A630)
- a630_gmu.bin - GPU 管理单元固件
- a630_sqe.fw - GPU 着色器引擎固件
- a630_zap.b00/b01/b02/elf/mdt - GPU ZAP 固件

#### 摄像头固件
- CAMERA_ICP.elf - 摄像头图像协处理器固件

#### IPA (IP Accelerator) 固件
- ipa_fws.b00-b04 - IPA 固件二进制文件
- ipa_fws.elf - IPA 固件 ELF 文件
- ipa_fws.mdt - IPA 固件元数据

#### Widevine DRM 固件
- widevine.b00-b07 - Widevine DRM 固件二进制文件
- widevine.mdt - Widevine DRM 固件元数据

#### DSP 固件

**ADSP (Audio DSP) - 24个文件**
- 音频解码器: AlacDecoderModule.so.1, ApeDecoderModule.so.1, FlacDecoderModule.so.1, VorbisDecoderModule.so.1, WmaProDecoderModule.so.1, WmaStdDecoderModule.so.1
- 音频编码器: EtsiEaacPlusEncAndCmnModule.so.1, EtsiAmrWbPlusDecModule.so.1, HeaacDecoderModule.so.1
- 音频处理: AudioSphereModule.so.1, AudioContextDetection.so.1, fluence_voiceplus_module.so.1, VoiceWakeup_V2_Module.so.1
- 其他: CFCMModule.so.1, LdacModule.so.1, SAPlusCmnModule.so.1, SVACmnModule.so.1, mmecns_module.so.1
- FastRPC shell: fastrpc_shell_0
- 库文件: libsysmon_skel.so, libsysmondomain_skel.so, libstabilitydomain_skel.so

**CDSP (Compute DSP) - 17个文件**
- 视频后处理: libvpp_frc.so, libvpp_svc_skel.so, libvpp_diagtools.so
- 视频增强: libvpp_aie.so, libvpp_mvp.so, libvpp_nr.so, libvpp_qbr.so
- 基准测试: libbenchmark_skel.so
- C++ 库: libc++.so.1, libc++abi.so.1
- 其他: libhcp_rpc_skel.so, ubwcdma_dynlib.so, fastrpc_shell_3

**SDSP (Sensor DSP) - 21个文件**
- CHRE 框架: libchre_slpi_skel.so
- CHRE 驱动: chre_drv_bt.so, chre_drv_gf.so, chre_drv_loc.so, chre_drv_modem.so, chre_drv_wifi.so
- CHRE 应用: chre_app_coverage.so, chre_app_coverage2.so, chre_app_oem.so, chre_app_sensor_cfg.so
- FastRPC: libFastRPC_UTF_Forward_800s_sdm845_skel.so
- 传感器工具: cellinfo_tools.so, sensor_testapp_40.so, lowi_client.so
- 库文件: libsysmon_skel.so, libsysmondomain_skel.so, libstabilitydomain_skel.so, chre_pal.so

#### Venus 视频编解码固件
- venus.b00-b04 - Venus 视频编解码器固件
- venus.mdt - Venus 固件元数据

#### WiFi 固件
- wlanmdsp.mbn - QCA6174 WiFi 芯片 DSP 固件

### 固件位置

所有固件已集成到 system.img 的以下位置：

```
/lib/firmware/                    # 根目录固件
/lib/firmware/adsp/               # ADSP 固件
/lib/firmware/cdsp/               # CDSP 固件
/lib/firmware/sdsp/               # SDSP 固件
/lib/firmware/ipa/               # IPA 固件
```

### 固件验证

可以通过以下命令验证固件是否正确加载：

```bash
# 进入 ADB shell
adb shell

# 检查 GPU 固件
ls -l /lib/firmware/a630*

# 检查 DSP 固件
ls -l /lib/firmware/adsp/
ls -l /lib/firmware/cdsp/
ls -l /lib/firmware/sdsp/

# 检查 WiFi 固件
ls -l /lib/firmware/wlanmdsp.mbn

# 检查 Venus 固件
ls -l /lib/firmware/venus.*

# 检查内核日志中的固件加载信息
dmesg | grep -i firmware
```

### 固件来源

固件来源：
1. **安卓9系统分区备份**: 基础固件提取
2. **官方 OnePlus 6 固件包**: DSP、Modem、Venus 完整固件
3. **AGNOS 原有固件**: 通用固件和配置

---

## 故障排除

### 问题 1: 设备无法启动到 Fastboot 模式

**症状**: 按住电源键 + 音量减键无反应

**解决方案**:
1. 确保使用高质量 USB 数据线
2. 尝试不同的 USB 端口
3. 在电脑上执行: `lsusb` 检查设备是否被识别
4. 尝试从关机状态长按电源键 10 秒强制重启

### 问题 2: Fastboot 无法识别设备

**症状**: `fastboot devices` 无输出

**解决方案**:
```bash
# 1. 检查 USB 权限
ls -l /dev/bus/usb/

# 2. 创建 udev 规则
sudo nano /etc/udev/rules.d/51-android.rules

# 添加以下内容:
SUBSYSTEM=="usb", ATTR{idVendor}=="0fce", MODE="0666", GROUP="plugdev"

# 3. 重载 udev 规则
sudo udevadm control --reload-rules
sudo udevadm trigger

# 4. 重新连接设备
```

### 问题 3: 刷机过程中出现 "command not allowed" 错误

**症状**: 刷机时提示 `command not allowed`

**解决方案**:
1. 确认 Bootloader 已解锁
2. 重新解锁 Bootloader:
```bash
fastboot oem unlock
```

### 问题 4: System 镜像刷写失败

**症状**: `fastboot flash system` 失败

**解决方案**:
```bash
# 1. 先擦除 system 分区
fastboot erase system

# 2. 重新刷写
fastboot flash system output/system.img

# 3. 如果仍然失败，尝试转换镜像格式
# 使用 simg2img 工具转换稀疏镜像
```

### 问题 5: 设备启动后黑屏

**症状**: 刷机后设备启动但屏幕黑屏

**解决方案**:
1. 等待 5-10 分钟，系统可能在初始化
2. 尝试连接 ADB 检查系统状态:
```bash
adb shell dmesg | tail
```
3. 检查显示驱动是否正常加载

### 问题 6: Wi-Fi 无法工作

**症状**: 系统启动后无法连接 Wi-Fi

**解决方案**:
```bash
# 1. 检查 Wi-Fi 模块是否已加载
adb shell lsmod | grep wlan

# 2. 如果未加载，手动加载 (wlan.ko 已集成在 system.img 中)
adb shell insmod /usr/comma/wlan.ko

# 3. 检查 Wi-Fi 固件
adb shell ls /lib/firmware/ath10k/

# 4. 检查 Wi-Fi 服务状态
adb shell systemctl status wifi.service

# 5. 启动 Wi-Fi 服务
adb shell systemctl start wifi.service

# 6. 检查 Wi-Fi 接口
adb shell iwconfig
adb shell ifconfig
```

**说明**:
- wlan.ko Wi-Fi 驱动已集成在 system.img 的 `/usr/comma/wlan.ko`
- 系统启动时 `wifi.service` 会自动加载该驱动
- 如果 Wi-Fi 仍然无法工作，请检查固件文件是否完整

### 问题 7: 设备变砖 (无法启动)

**症状**: 设备完全无法启动，无法进入任何模式

**解决方案**:
1. 尝试进入 EDL 模式 (9008 模式)
2. 使用 Qualcomm 工具刷入完整固件
3. 联系 OnePlus 官方售后

---

## 常见问题

### Q1: 刷机后还能回到官方系统吗?

**A**: 可以，但需要:
1. 下载 OnePlus 6 官方 ROM
2. 使用 fastboot 刷入官方 boot.img 和 system.img
3. 重新锁定 Bootloader (可选)

### Q2: AGNOS 系统会自动更新吗?

**A**: AGNOS 不会自动更新。更新需要:
1. 重新构建镜像
2. 使用 fastboot 刷入新镜像

### Q3: 刷机后数据会丢失吗?

**A**: 是的，刷机会清除:
- userdata 分区 (所有应用数据)
- cache 分区
- 部分系统设置

建议在刷机前备份重要数据。

### Q4: AGNOS 支持哪些功能?

**A**: AGNOS 支持的功能包括:
- Ubuntu 20.04 基础系统
- Openpilot 自动驾驶功能
- Wi-Fi 连接
- 触摸屏输入
- 摄像头访问
- 传感器数据读取

不支持的功能:
- 蜂窝网络 (电话/短信)
- 蓝牙 (部分支持)
- GPS (部分支持)

### Q5: 刷机需要多长时间?

**A**: 预计时间:
- Boot 镜像刷写: 1-2 分钟
- System 镜像刷写: 5-10 分钟
- 首次启动: 3-5 分钟

总计约 10-20 分钟。

### Q6: 如何回滚到之前的版本?

**A**: 如果有备份的镜像文件:
```bash
# 刷入之前的 boot.img
fastboot flash boot backup/boot.img

# 刷入之前的 system.img
fastboot flash system backup/system.img

# 重启
fastboot reboot
```

### Q7: 刷机后如何获取日志?

**A**: 获取系统日志:
```bash
# 获取内核日志
adb shell dmesg > kernel.log

# 获取系统日志
adb shell journalctl > system.log

# 获取 openpilot 日志
adb shell journalctl -u openpilot > openpilot.log
```

---

## 附录

### A. 刷机脚本详解

#### flash_all.sh

一键刷机脚本，自动执行所有刷机步骤。

**主要功能**:
- 检查 fastboot 工具
- 验证设备连接和型号
- 格式化 userdata 分区
- 刷写 boot 镜像
- 刷写 system 镜像
- 刷写 vendor 镜像 (可选)
- 重启设备

**使用方法**:
```bash
./flash_all.sh
```

#### wlan.ko Wi-Fi 驱动说明

**集成方式**:
- wlan.ko Wi-Fi 驱动已集成在 `system.img` 中
- 位置: `/usr/comma/wlan.ko`
- 构建时从内核源码编译并复制到系统镜像

**WiFi 固件**:
- wlanmdsp.mbn (QCA6174 WiFi 芯片 DSP 固件) 已集成到 `/lib/firmware/`
- 固件来源: 官方 OnePlus 6 固件包
- DSP 固件与驱动配合工作，提供完整的 WiFi 功能支持

**自动加载**:
- 系统启动时 `wifi.service` 会自动加载驱动
- 服务文件: `/lib/systemd/system/wifi.service`
- 执行命令: `/sbin/insmod /usr/comma/wlan.ko`

**手动加载**:
```bash
# 如果需要手动加载
adb shell insmod /usr/comma/wlan.ko

# 检查是否加载成功
adb shell lsmod | grep wlan

# 检查 WiFi 固件
adb shell ls -l /lib/firmware/wlanmdsp.mbn

# 检查 WiFi 接口
adb shell iwconfig
adb shell ifconfig
```

**其他内核模块**:
- `/usr/comma/sound/snd-soc-sdm845.ko` - 音频驱动
- `/usr/comma/sound/snd-soc-wcd9xxx.ko` - 音频 codec 驱动
- 这些模块也集成在 system.img 中

#### flash_kernel.sh

仅刷写内核镜像。

**主要功能**:
- 刷写 boot_a 和 boot_b 分区
- 用于快速更新内核

**使用方法**:
```bash
./flash_kernel.sh
```

#### flash_system.sh

仅刷写系统镜像。

**主要功能**:
- 设置活动分区为 A
- 刷写 system_a 分区
- 自动重启设备

**使用方法**:
```bash
./flash_system.sh
```

### B. 分区布局

#### OnePlus 6 原始分区布局（未调整）

| 分区名称 | 设备路径 | 扇区范围 | 大小 | 文件系统 | 用途 |
|---------|---------|---------|------|---------|------|
| ssd | /dev/sda1 | 6-7 | 8.0 KiB | - | 系统安全数据 |
| persist | /dev/sda2 | 8-8199 | 32.0 MiB | ext4 | 持久化数据分区 |
| misc | /dev/sda3 | 8200-8455 | 1024.0 KiB | - | 杂项数据 |
| param | /dev/sda4 | 8456-8711 | 1024.0 KiB | - | 参数存储 |
| keystore | /dev/sda5 | 8712-8839 | 512.0 KiB | - | 密钥存储 |
| frp | /dev/sda6 | 8840-8967 | 512.0 KiB | - | 工厂重置保护 |
| op2 | /dev/sda7 | 8968-74503 | 256.0 MiB | ext4 | 额外分区2 |
| oem_dycnvbk | /dev/sda8 | 74504-77063 | 10.0 MiB | - | OEM动态证书备份 |
| oem_stanvbk | /dev/sda9 | 77064-79623 | 10.0 MiB | - | OEM静态证书备份 |
| reserve1 | /dev/sda10 | 79624-81647 | 7.9 MiB | - | 保留分区1 |
| reserve2 | /dev/sda11 | 81648-85695 | 15.8 MiB | - | 保留分区2 |
| config | /dev/sda12 | 85696-85823 | 512.0 KiB | - | 配置分区 |
| system_a | /dev/sda13 | 85824-817983 | 2.8 GiB | ext4 | Android系统分区A |
| system_b | /dev/sda14 | 817984-1550143 | 2.8 GiB | ext4 | Android系统分区B |
| odm_a | /dev/sda15 | 1550144-1575743 | 100.0 MiB | ext4 | ODM分区A |
| odm_b | /dev/sda16 | 1575744-1601343 | 100.0 MiB | ext4 | ODM分区B |
| userdata | /dev/sda17 | 1601344-30437370 | 110.0 GiB | ext4 | 用户数据分区 |

#### AGNOS 调整后分区布局（推荐）

| 分区名称 | 设备路径 | 扇区范围 | 大小 | 文件系统 | 变化 |
|---------|---------|---------|------|---------|------|
| system_a | /dev/sda13 | 85824-4194303 | 15 GiB | ext4 | 2.8GB → 15GB (+12.2GB) |
| system_b | /dev/sda14 | 4194304-8323327 | 15 GiB | ext4 | 2.8GB → 15GB (+12.2GB) |
| userdata | /dev/sda17 | 8323328-30437370 | 85 GiB | ext4 | 110GB → 85GB (-25GB) |
| odm_a | 删除 | - | - | - | 已删除 |
| odm_b | 删除 | - | - | - | 已删除 |

**重要说明**：
- AGNOS system.img 刷写到 system_a 或 system_b 分区
- userdata 分区 (85GB) 用于存储用户数据和应用
- OnePlus 6 使用 A/B 分区方案，system_a 和 system_b 互为备份
- AGNOS 系统启动时会挂载 system_a 或 system_b 作为根文件系统
- **必须先执行分区调整步骤**，否则 system 分区空间不足

### C. 常用 Fastboot 命令

```bash
# 查看设备信息
fastboot devices
fastboot getvar all

# 刷写分区
fastboot flash boot boot.img
fastboot flash system system.img
fastboot flash vendor vendor.img

# 擦除分区
fastboot erase userdata
fastboot erase cache

# 格式化分区
fastboot format:ext4 userdata

# 重启设备
fastboot reboot
fastboot reboot-bootloader
fastboot continue

# 解锁/锁定 Bootloader
fastboot oem unlock
fastboot flashing unlock
fastboot oem lock
fastboot flashing lock
```

### D. 常用 ADB 命令

```bash
# 查看设备
adb devices

# 进入 shell
adb shell

# 传输文件
adb push local_file /remote/path/
adb pull /remote/file local_path/

# 安装应用
adb install app.apk

# 查看日志
adb logcat
adb shell dmesg
adb shell journalctl

# 重启设备
adb reboot
adb reboot bootloader
adb reboot recovery

# 截图
adb screencap screenshot.png

# 录屏
adb screenrecord video.mp4
```

---

## 参考资料

- [Openpilot 官方文档](https://github.com/commaai/openpilot)
- [AGNOS 构建文档](https://github.com/commaai/agnos)
- [OnePlus 6 开发者文档](https://forum.xda-developers.com/oneplus6)
- [Android Fastboot 文档](https://source.android.com/devices/bootloader/fastboot)

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.1 | 2026-01-22 | 更新固件说明，添加完整的固件清单和验证方法 |
| 1.0 | 2026-01-22 | 初始版本，基于 AGNOS 5.1 |

---

## 联系方式

如有问题或建议，请通过以下方式联系:
- GitHub Issues: [项目地址]
- 邮箱: [联系邮箱]

---

**免责声明**: 本文档仅供学习和研究使用。刷机操作可能导致设备损坏或失去保修，请自行承担风险。作者不对任何损失负责。

**最后更新**: 2026-01-22
